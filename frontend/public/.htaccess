RewriteEngine On

# Allow direct access to static files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Forward everything else to Node.js app
RewriteRule ^(.*)$ https://intern.bhorajmalla.com.np$1 [P,L]
